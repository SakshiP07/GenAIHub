apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: genaihub-backend
spec:
  type: ClusterIP
  selector:
    app: genaihub-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
